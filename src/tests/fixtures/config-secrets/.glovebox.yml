main:
  sandbox_image: buildpack-deps:noble   # curl preinstalled
  secrets:
    - "GLOVEBOX_TEST_TOKEN=$GLOVEBOX_TEST_TOKEN_HOST@httpbin.org"
  tls_intercept: true
  trust_host_cas: true
  network:
    - httpbin.org
  pass_env: none
