---
name: test-agent
description: Test agent for verifying agent-specific sandbox image selection
tools: ["Bash"]
---

Run `cat /etc/os-release` using Bash and report the exact value of the NAME= line.
